{
  "name": "design-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/server.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=18" }
}
